# UI_SPEC

Milestone 07 のスマホ操作 UI。ゲームの論理解像度は 1280×720 のまま。ボタンの位置は、表示されたキャンバスと CSS の Safe Area からゲーム座標へ換算する。

## 端末

タッチ UI と縦向き案内は `isTouchLayout()` が true のときだけ使う。

- `(pointer: coarse)` である
- かつ `(hover: none)` または `navigator.maxTouchPoints > 0`

PC では仮想スティック、タッチボタン、縦向き案内を出さない。キーボードの割り当ては変えない。

縦向きは、上の条件に加えて `(orientation: portrait)`、または高さが幅より 40 CSS px 以上大きいとき。案内文は「端末を横向きにしてください」。横向きに戻ると class `is-visible` を外して消える。

## 右手

必殺を頂点、ジャンプを左下、攻撃を右下に置く。

- タッチ領域の直径は 64 CSS px。見た目の円はその 0.84 倍で、判定の方が少し広い
- ボタン中心の間隔は、直径に 14 CSS px を足した距離。上下の間隔はその正三角形の高さ
- 画面端と Safe Area から 18 CSS px 以上内側
- 攻撃は赤、必殺は紫、ジャンプは青。ラベルは黒
- 押しているあいだは 0.92 倍に縮み、塗りを明るくし、枠を白く太くする

## 左手

仮想スティックは左下に固定する。

- 外円の直径は 108 CSS px。ノブは 46 CSS px
- 掴める範囲は外円半径の 1.28 倍
- 入力中はノブが指の方向へ動き、外円の枠が太くなる
- 指を離すと中央へ戻る

## HUD

`BattleHud` が半透明の角丸パネルを出す。左上はもちまる、右上はぽてち。中身は色付きの丸、名前、残ストックの丸、ダメージ%。

- 0〜49%: 40px。もちまるは青、ぽてちは茶色
- 50〜99%: 46px、太字、`#e07a00`
- 100% 以上: 54px、太字、`#d01212`
- PC: パネルの外側を左 (24, 16)、右 (1256, 16)
- スマホ: Safe Area と 16 CSS px の内側。操作 UI は下のまま

## ヒットと勝敗

`HitEffectView` がヒット位置に火花を約 280ms 出す。判定は変えない。通常攻撃は小さく、上必殺は中、ぐるぐる突進とどっすーん！は大きい。被弾中の白表示は 100ms。

KO は画面内に短い光と「KO!」を出す。復活や勝敗の待ち時間は足さない。勝敗パネルは「もちまる WIN」または「ぽてち WIN」と、R キー / タップの案内。

## ブラウザの標準操作

ゲームキャンバスと縦向き案内だけを対象にする。

- `touch-action: none`
- `overscroll-behavior: none`
- 選択と長押しメニューを抑える
- `viewport-fit=cover` と `user-scalable=no`

## 責務

- `TouchInput`: `PlayerInputState` を作る。ポインタ ID を持つ
- `TouchControlsView`: スティックとボタンの見た目、当たり判定の座標
- `touchControlLayout`: ビューポートから配置を計算する
- `BattleHud`: 名前、ストック、ダメージ% のパネル
- `MobileHudLayout`: そのパネルを Safe Area の内側へ動かす
- `HitEffectView`: ヒットの火花
- `MatchResultView`: KO の短い表示と勝敗パネル
- `deviceLayout`: タッチ判定、縦向き、Safe Area、キャンバス枠
